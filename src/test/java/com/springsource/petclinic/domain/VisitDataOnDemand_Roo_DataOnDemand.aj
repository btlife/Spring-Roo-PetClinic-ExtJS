// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.springsource.petclinic.domain;

import com.springsource.petclinic.domain.PetDataOnDemand;
import com.springsource.petclinic.domain.Visit;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect VisitDataOnDemand_Roo_DataOnDemand {
    
    declare @type: VisitDataOnDemand: @Component;
    
    private Random VisitDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Visit> VisitDataOnDemand.data;
    
    @Autowired
    private PetDataOnDemand VisitDataOnDemand.petDataOnDemand;
    
    public Visit VisitDataOnDemand.getNewTransientVisit(int index) {
        com.springsource.petclinic.domain.Visit obj = new com.springsource.petclinic.domain.Visit();
        java.lang.String description = "description_" + index;
        if (description.length() > 255) {
            description  = description.substring(0, 255);
        }
        obj.setDescription(description);
        obj.setVisitDate(new java.util.Date(new java.util.Date().getTime() - 10000000L));
        obj.setPet(petDataOnDemand.getRandomPet());
        obj.setVet(null);
        return obj;
    }
    
    public Visit VisitDataOnDemand.getSpecificVisit(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Visit obj = data.get(index);
        return Visit.findVisit(obj.getId());
    }
    
    public Visit VisitDataOnDemand.getRandomVisit() {
        init();
        Visit obj = data.get(rnd.nextInt(data.size()));
        return Visit.findVisit(obj.getId());
    }
    
    public boolean VisitDataOnDemand.modifyVisit(Visit obj) {
        return false;
    }
    
    public void VisitDataOnDemand.init() {
        data = com.springsource.petclinic.domain.Visit.findVisitEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Visit' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<com.springsource.petclinic.domain.Visit>();
        for (int i = 0; i < 10; i++) {
            com.springsource.petclinic.domain.Visit obj = getNewTransientVisit(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
